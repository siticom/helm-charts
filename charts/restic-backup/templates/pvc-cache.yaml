{{- if .Values.restic.cache.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "restic-backup.fullname" . }}-cache
  {{- with .Release.Namespace }}
  namespace: {{ toYaml . }}
  {{- end }}
  labels:
    {{- include "restic-backup.labels" . | nindent 4 }}
spec:
  accessModes: {{ .Values.restic.cache.accessModes }}
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.restic.cache.size }}
  {{- with .Values.restic.cache.storageClass }}
  storageClassName: {{ toYaml . }}
  {{- end }}
{{- end }}
